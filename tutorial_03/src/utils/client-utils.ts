import "client-only"

export const ClientUtils = () => {
    console.log(
        `use window object,
        use event listener,
        use state and effects,
        use localStorage`
    );
    return "client result";
}